apiVersion: v1
kind: Secret
metadata:
  name: {{ include "boxy.fullname" . }}-tokens
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  router-token: {{ .Values.routerToken | quote }}
  worker-token: {{ .Values.workerToken | quote }}
