{{- if not .Values.mtls.disabled }}
{{- $fullname := include "boxy.fullname" . -}}
{{- $existing := lookup "v1" "Secret" .Release.Namespace (printf "%s-mtls-controller" $fullname) }}
{{- if not $existing }}
{{- $ca := genCA .Values.mtls.caCN 3650 }}
{{- $serverDNS := list .Values.mtls.serverCN (printf "*.%s-ctrl-headless.%s.svc" $fullname .Release.Namespace) (printf "*.%s-ctrl-headless.%s.svc.cluster.local" $fullname .Release.Namespace) }}
{{- $serverCert := genSignedCert .Values.mtls.serverCN nil $serverDNS 3650 $ca }}
{{- $clientDNS := list .Values.mtls.clientCN }}
{{- $clientCert := genSignedCert .Values.mtls.clientCN nil $clientDNS 3650 $ca }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $fullname }}-mtls-controller
  namespace: {{ .Release.Namespace }}
  annotations:
    helm.sh/resource-policy: keep
type: kubernetes.io/tls
data:
  ca.crt: {{ $ca.Cert | b64enc | quote }}
  tls.crt: {{ $serverCert.Cert | b64enc | quote }}
  tls.key: {{ $serverCert.Key | b64enc | quote }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $fullname }}-mtls-router
  namespace: {{ .Release.Namespace }}
  annotations:
    helm.sh/resource-policy: keep
type: kubernetes.io/tls
data:
  ca.crt: {{ $ca.Cert | b64enc | quote }}
  tls.crt: {{ $clientCert.Cert | b64enc | quote }}
  tls.key: {{ $clientCert.Key | b64enc | quote }}
{{- end }}
{{- end }}
